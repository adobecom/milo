/**
 * C2 Foundation — Merch Card
 *
 * Thin wrapper around the base merch-card block.
 * The framework auto-loads merch-card.css from this directory when
 * foundation=c2 is set, applying --s2a-* token overrides on top of
 * the shared merch-card logic.
 */

import baseInit from '../../../blocks/merch-card/merch-card.js';

export default baseInit;
