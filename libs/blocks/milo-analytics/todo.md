1. find a better way to filter the urls by project
2. get domain keys for bacom, blog, helpx, news
3. clean up the ui
4. add report sharing feature
    4.1 Could save the selected filters in query parameters and share the url. The page will auto-populate the filters
    4.2 Could save the results in a database, create a new block that will process them based on a reference id